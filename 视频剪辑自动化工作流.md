# 视频剪辑自动化工作流

> 基于"皇马欧冠三连冠怀念视频"项目（v1→v3.2）实战经验抽象而成。每一步包含操作说明、涉及工具、输入要求、输出产物，可直接按流程执行。

---

## 0. 环境前置检查（首次使用必做）

### 操作说明
检查本机硬件能力、软件环境、网络可达性，决定后续编码方案与素材源。

### 涉及工具
- `ffmpeg` / `ffprobe`（核心编码工具）
- `yt-dlp`（视频下载）
- Python venv（隔离环境）
- GPU 诊断：`nvidia-smi` / `Get-CimInstance Win32_VideoController`

### 检查清单

| 检查项 | 命令 | 决策用途 |
|---|---|---|
| GPU 型号 | PowerShell `Get-CimInstance Win32_VideoController` | 选 QSV/NVENC/AMF 硬编码 |
| ffmpeg 硬件编码器 | `ffmpeg -encoders \| grep -iE "nvenc\|qsv\|amf"` | 确认可用编码器 |
| QSV 可用性测试 | `ffmpeg -f lavfi -i testsrc=duration=1:size=320x240 -c:v h264_qsv -f null -` | 验证 Intel 核显/Arc |
| 浏览器 cookies | `ls AppData/Local/Microsoft/Edge/User Data/Default/Cookies` | 用于 B站登录态下载 |
| 素材源连通性 | `curl -s -o /dev/null -w "%{http_code}" --max-time 8 https://www.bilibili.com` | YouTube=000 时改用 B站 |
| 剪映安装 | 查开始菜单/AppData | GUI 工具，无法程序化调用 |

### 输出产物
- 一份环境清单：GPU 型号、可用硬编码器、可达素材源、是否需要登录

### 关键决策树
```
有 NVIDIA GPU？ → 用 h264_nvenc（速度最快）
└─ 有 Intel Arc/核显？ → 用 h264_qsv（推荐，CPU占用~20%）
   └─ 有 AMD GPU？ → 用 h264_amf
      └─ 都没有 → 用 libx264 -preset fast（CPU占用~90%，质量最好）
```

**环境切换注意（v1.1 实测）**：更换电脑/显卡后必须重新执行第 0 步。台式机 QSV → 笔记本 NVENC 切换经验：
- ffmpeg 7.1 完整版（gyan.dev）自带全部硬编码器（nvenc/qsv/amf），无需重装，用 `ffmpeg -encoders | grep -iE "nvenc|qsv|amf"` 确认
- h264_nvenc 的质量参数是 `-cq <值>`（≈QSV 的 `-global_quality`，20 ≈ CRF 18-20），预置用 `-preset p5`（p7 最快但质量降）
- NVENC 恒定质量推荐写法：`-c:v h264_nvenc -rc vbr -cq 20 -b:v 0 -preset p5`（`-b:v 0` 让码率自适应）
- 装机踩坑：清华源（tuna）对部分小包会 403 Forbidden，此时裸跑 `pip install`（官方源）即可

### 安装命令（首次）
```bash
# Python venv + 工具链
"C:/Users/<user>/.workbuddy/binaries/python/versions/3.13.12/python.exe" \
  -m venv "C:/Users/<user>/.workbuddy/binaries/python/envs/default"
"C:/Users/<user>/.workbuddy/binaries/python/envs/default/Scripts/pip.exe" \
  install -i https://pypi.tuna.tsinghua.edu.cn/simple yt-dlp imageio-ffmpeg

# ffmpeg 路径（imageio-ffmpeg 自带）
FFMPEG="C:/Users/<user>/.workbuddy/binaries/python/envs/default/Lib/site-packages/imageio_ffmpeg/binaries/ffmpeg-win-x86_64-v7.1.exe"
```

---

## 1. 素材搜集阶段

### 操作说明
按主题关键词，从可达平台搜索并下载高清视频素材。优先 1080P 免登录源。

### 涉及工具
- `WebSearch`（找 BV 号/关键词）
- `WebFetch`（抓取 B站搜索页提取 BV 号）
- `yt-dlp`（下载视频流）
- `ffmpeg`（合并音视频流）

### 输入要求
- 视频主题关键词（如"皇马 欧冠三连冠 集锦"）
- 目标分辨率（1080P 优先）
- 时长预算（如 4 分钟成片需 30-50 分钟素材）

### 输出产物
- `assets/raw/<BV号>.mp4`（合并后的源视频）
- 素材清单表（BV号、标题、时长、分辨率、内容摘要）

### 1.1 关键词搜索策略

```bash
# B站搜索（YouTube 在大陆不可达时首选）
yt-dlp "bilisearch:<关键词>" --flat-playlist --print "%(id)s | %(title)s | %(duration)s" -I 1:12
```

**关键词组合模板**（按主题扩展）：
- 主体 + 动作 + 修饰：`皇马 进球 集锦`
- 主体 + 事件 + 时间：`皇马 欧冠决赛 2018`
- 主体 + 风格：`皇马 纪录片 高清`

### 1.2 素材筛选标准

| 维度 | 标准 | 检查方法 |
|---|---|---|
| 分辨率 | ≥1080P（1920×1080）| `yt-dlp -F <url>` 看 30080 格式 |
| 帧率 | 25/30fps（避免 60fps 浪费空间）| `yt-dlp -F` |
| 编码 | H.264 优先（兼容性好）| `ffmpeg -i` 看 VCODEC |
| 时长 | 单源 ≥3 分钟（够剪辑用）| `yt-dlp -F` 看 duration |
| 版权 | 个人使用，不公开发布 | UP主声明 |

### 1.3 下载命令模板

```bash
# 列出可用格式（不下载）
yt-dlp -F "https://www.bilibili.com/video/<BV号>"

# 下载 1080P 视频流 + 音频流（分开，因 yt-dlp postprocessing 需 ffmpeg）
yt-dlp -f "30080" -o "assets/raw/<BV号>.f30080.mp4" "https://www.bilibili.com/video/<BV号>"
yt-dlp -f "30280" -o "assets/raw/<BV号>.f30280.m4a" "https://www.bilibili.com/video/<BV号>"

# 合并视频+音频
"$FFMPEG" -y -i "<BV号>.f30080.mp4" -i "<BV号>.f30280.m4a" -c copy "<BV号>.mp4"

# 登录下载（1080P高码率需大会员）
yt-dlp --cookies-from-browser edge -f "bestvideo[height<=1080]+bestaudio" "https://www.bilibili.com/video/<BV号>"
```

### 1.4 音乐素材获取
```bash
# B站无损音乐源
yt-dlp -f bestaudio -x --audio-format mp3 --audio-quality 0 \
  -o "assets/audio/<歌名>.%(ext)s" "https://www.bilibili.com/video/<BV号>"
# 注：--ffmpeg-location 在 postprocessing 阶段不生效，需手动转码
# m4a → mp3: ffmpeg -y -i input.m4a -codec:a libmp3lame -qscale:a 2 output.mp3
```

### 自动化机会
- ✅ 批量搜索：`bilisearch:` 一次返回多个结果
- ✅ 批量下载：循环遍历 BV 号列表
- ⚠️ 内容筛选：需人工截帧确认（见 1.5）

### 1.5 内容确认（半自动）
```bash
# 截取关键帧查看内容分布
for t in 00:00:15 00:02:00 00:04:30 00:07:00; do
  "$FFMPEG" -y -ss $t -i <源>.mp4 -frames:v 1 -q:v 3 -vf scale=480:-1 "frames/<源>_${t//:/}.jpg"
done
```

---

## 2. 素材整理阶段

### 操作说明
建立统一目录结构和命名规范，确保每个素材可追溯来源。

### 涉及工具
- 文件系统（mkdir/ls）
- 文本记录（素材清单.md）

### 输入要求
- 下载的原始素材文件

### 输出产物
- 标准化目录结构
- 素材清单文档

### 2.1 目录结构规范

```
<项目根>/
├── assets/
│   ├── raw/              # 原始下载视频（合并后）
│   │   ├── BV11o4y1o7UK.mp4
│   │   ├── BV1DHWcz8EQE.mp4
│   │   └── BV1at4y1p7Xv.f30080.mp4
│   ├── audio/            # 配乐音频
│   │   ├── last_summer.mp3
│   │   └── last_summer.m4a
│   └── frames/           # 内容分析用截帧（临时）
├── clips/                # 截取的片段（临时，编码后可删）
├── output/               # 最终输出
│   └── <项目名>_v3.mp4
└── 剪辑方案.md           # 分镜设计文档
```

### 2.2 命名规范

| 类型 | 命名规则 | 示例 |
|---|---|---|
| 原始视频 | `<BV号>.mp4` | `BV11o4y1o7UK.mp4` |
| 视频流 | `<BV号>.f<格式ID>.mp4` | `BV1at4y1p7Xv.f30080.mp4` |
| 音频流 | `<BV号>.f<格式ID>.m4a` | `BV11o4y1o7UK.f30280.m4a` |
| 片段 | `<序号>.mp4`（2位）| `01.mp4`, `02.mp4` |
| 截帧 | `<源>_<时间>.jpg` | `g20_000200.jpg` |
| 最终输出 | `<主题>_v<版本>.mp4` | `皇马_欧冠三连冠_怀念_v3.2.mp4` |

### 2.3 素材清单模板

```markdown
## 素材清单

### 视频源
| BV号 | 标题 | 时长 | 分辨率 | 帧率 | 编码 | 内容摘要 |
|---|---|---|---|---|---|---|
| BV11o4y1o7UK | 20大进球 | 9:16 | 1920×1080 | 25 | H.264 | 进球集锦 |
| BV1DHWcz8EQE | 神仙倒钩 | 24:22 | 1916×1080 | 30 | AV1 | 比赛回顾 |

### 音频源
| 文件 | 歌曲 | 时长 | BPM | 来源 |
|---|---|---|---|---|
| last_summer.mp3 | 去年夏天-王大毛 | 4:03 | 128 | BV1gt69BNE3v |
```

---

## 3. 剪辑制作阶段

### 操作说明
设计视频结构，截取片段，拼接成片。采用"两段式叙事"提升层次感。

### 涉及工具
- `ffmpeg`（截取、拼接、转场）
- 截帧分析（内容定位）

### 输入要求
- 原始素材视频
- 配乐音频
- 主题与风格要求

### 输出产物
- `clips/01.mp4` ~ `clips/NN.mp4`（截取的片段）
- 剪辑方案文档（分镜表）

### 3.1 视频结构模板

```
┌─────────────────────────────────────────┐
│  片头（5-10s）：标题/引子/空镜           │
├─────────────────────────────────────────┤
│  第一段（40-50%时长）：高燃/主体内容     │
│  → 进球、动作、快节奏                    │
├─────────────────────────────────────────┤
│  第二段（40-50%时长）：情感/回忆内容     │
│  → 庆祝、捧杯、慢镜头                    │
├─────────────────────────────────────────┤
│  片尾（5-10s）：渐隐/留白/字幕           │
└─────────────────────────────────────────┘
```

### 3.2 分镜设计表模板

| 序号 | 时段 | 时长 | 源视频 | 入点 | 内容 | 配乐段落 |
|---|---|---|---|---|---|---|
| 01 | 0:00-0:22 | 22s | BV1DHWcz8EQE | 6:00 | C罗远射 | 主歌1 |
| 02 | 0:22-0:42 | 20s | BV11o4y1o7UK | 2:00 | vs多特 | 主歌1 |
| ... | ... | ... | ... | ... | ... | ... |

### 3.3 片段截取命令（QSV硬编码）

```bash
FFMPEG="<ffmpeg路径>"
SRC="assets/raw/<源>.mp4"

# 单片段截取 + 统一编码（1080P/25fps/H.264）
"$FFMPEG" -y -ss <入点秒> -i "$SRC" -t <时长秒> \
  -vf "scale=1920:1080,fps=25,format=yuv420p" \
  -c:v h264_qsv -preset fast -global_quality 20 -an \
  clips/<序号>.mp4
```

**参数说明**：
- `-ss` 在 `-i` 前：快速 seek（基于关键帧）
- `-t`：片段时长（秒）
- `scale=1920:1080`：统一分辨率（16:9 不变形）
- `fps=25`：强制 25fps CFR（**避免变帧率卡顿**）
- `-an`：去除原声（用统一 BGM）
- `-global_quality 20`：QSV 的 ICQ 质量（≈CRF 18-20）

### 3.4 去水印（delogo）

```bash
# 单水印去除
"$FFMPEG" -y -ss <入点> -i "$SRC" -t <时长> \
  -vf "scale=1920:1080,fps=25,format=yuv420p,delogo=x=<X>:y=<Y>:w=<W>:h=<H>" \
  -c:v h264_qsv -preset fast -global_quality 20 -an \
  clips/<序号>.mp4

# 多水印去除（叠加 delogo）
-vf "...,delogo=x=1280:y=5:w=620:h=100,delogo=x=10:y=10:w=70:h=50"
```

**水印位置测量**：
```bash
# 裁出右上角放大看
"$FFMPEG" -y -ss <时间> -i "$SRC" -frames:v 1 \
  -vf "crop=300:200:1620:0,scale=1200:800" corner.jpg
```

### 3.5 拼接（concat demuxer）

```bash
# 生成文件列表
printf "file 'clips/%02d.mp4'\n" {1..12} > clips.txt

# 流复制拼接（参数统一时最快）
"$FFMPEG" -y -f concat -safe 0 -i clips.txt -c copy output/concat.mp4
```

**已切片素材直拼（v1.1 实测，秒级完成）**：若所有片段编码参数完全一致（分辨率/帧率/编码器/像素格式/音频规格），可直接 `-c copy` 流复制拼接，**零画质损失、约 128x 速度**（489MB 素材约 3 秒出片）。ffmpeg 会自动插入 `h264_mp4toannexb` bitstream filter。
- **验证方法**：拼接前逐个 `ffmpeg -i` 核对 `Duration / Stream #` 两行是否完全一致（含 SAR、tbn、音频采样率/声道）
- **建议追加** `-movflags +faststart`（moov 前置，网络播放秒开；流复制时同样适用）
- 若参数不一致（如 H.264+AV1 混源），必须退回 3.3 统一编码后再拼，否则报 concat 错误

### 3.6 转场风格

| 转场类型 | ffmpeg 命令 | 适用场景 |
|---|---|---|
| 硬切 | 默认（无滤镜） | 快节奏段落 |
| 淡入淡出 | `-vf "fade=t=in:st=0:d=0.5,fade=t=out:st=<T-0.5>:d=0.5"` | 段落过渡 |
| 末尾渐隐 | `-vf "fade=t=out:st=230:d=13"` | 片尾收束 |

### 3.7 配乐对齐逻辑

```bash
# 配乐时长 = 视频时长（用 -t 限制）
"$FFMPEG" -y -i output/concat.mp4 -i assets/audio/<BGM>.mp3 \
  -t <BGM时长秒> \
  -c:v <编码器> ... \
  -c:a aac -b:a 192k \
  -af "afade=t=out:st=<T-13>:d=13" \
  -movflags +faststart \
  output/<最终>.mp4
```

- `-t`：限制输出时长 = BGM 时长
- `afade`：末尾音频渐弱
- `fade`（视频）：末尾画面渐黑
- `-movflags +faststart`：MP4 faststart（网络播放秒开）

### 3.8 字幕烧录（v1.1 新增，中文字幕实测）

```bash
# 外挂字幕生成（SRT，时间轴按片段时长自动累计——用 Python 脚本，勿手算）
#   durations = [每段时长秒]；lines = [每段文案]；累计出 starts；即可输出 SRT

# 烧录进画面（重新编码，libass 渲染）
"$FFMPEG" -y -i input.mp4 \
  -vf "subtitles=subs.srt:force_style='FontName=Microsoft YaHei,FontSize=22,PrimaryColour=&H00FFFFFF,OutlineColour=&H00000000,BorderStyle=1,Outline=2,Shadow=1,MarginV=80'" \
  -c:v h264_nvenc -rc vbr -cq 20 -b:v 0 -preset p5 \
  -c:a copy -movflags +faststart \
  output/video_v2_sub.mp4
```

**参数说明与坑**：
- 中文字体用 `FontName=Microsoft YaHei`（微软雅黑），gyan.dev 完整版 ffmpeg（含 libass + DirectWrite）自动从系统加载，无需指定字体文件
- `PrimaryColour=&H00FFFFFF` 白字 / `OutlineColour=&H00000000` 黑边 / `Outline=2,Shadow=1` 描边阴影 / `MarginV=80` 距底边距
- **Windows 路径坑**：subtitles 滤镜对含 `:`（盘符）和 `\` 的路径敏感，最稳妥做法是 `cd` 到输出目录、用相对文件名
- 烧录 = 重新编码：原片（v1）保留，字幕版另存（v2），符合版本管理原则
- 播放器可关字幕的场景优先用外挂 SRT（零画质损失）；发布/直传平台用烧录版

---

## 4. 输出导出阶段

### 操作说明
按目标平台规格导出最终视频，多版本并存。

### 涉及工具
- `ffmpeg`（编码封装）

### 输入要求
- 拼接后的无声视频
- 配乐音频
- 目标平台规格

### 输出产物
- 最终视频文件（保留所有历史版本）

### 4.1 导出参数表

| 平台 | 分辨率 | 帧率 | 码率 | 编码 | 音频 | 格式 |
|---|---|---|---|---|---|---|
| 短视频（抖音/B站） | 1080×1920 竖屏 | 30fps | 4-6 Mbps | H.264 | AAC 128k | MP4 |
| 长视频（B站/YouTube） | 1920×1080 横屏 | 25/30fps | 5-8 Mbps | H.264 | AAC 192k | MP4 |
| 高质量存档 | 1920×1080 | 25fps | 8-10 Mbps | H.264 CRF 18 | AAC 192k | MP4 |
| 4K | 3840×2160 | 30fps | 25-40 Mbps | H.265/HEVC | AAC 256k | MP4 |

### 4.2 标准导出命令

```bash
# QSV 硬编码 + BGM + fade（推荐）
"$FFMPEG" -y -i output/concat.mp4 -i assets/audio/<BGM>.mp3 \
  -t <时长> \
  -c:v h264_qsv -preset fast -global_quality 20 \
  -pix_fmt yuv420p \
  -vf "fade=t=out:st=<T-13>:d=13" \
  -c:a aac -b:a 192k \
  -af "afade=t=out:st=<T-13>:d=13" \
  -movflags +faststart \
  output/<项目名>_v<版本>.mp4
```

### 4.3 版本管理规范

```
output/
├── <项目名>.mp4                  # v1 初版
├── <项目名>_v2.mp4               # 修复版
├── <项目名>_v3_QSV.mp4           # QSV硬编码版
├── <项目名>_v3.1_nologo.mp4      # 去水印版
└── <项目名>_v3.2_nologo.mp4      # 最终版
```

**原则**：原始视频文件不做任何删除或覆盖，每个版本独立文件。

### 4.4 平台适配快速转换

```bash
# 横屏 → 竖屏（裁剪中间）
"$FFMPEG" -y -i input.mp4 -vf "crop=1080:1920:420:0,scale=1080:1920" output_vertical.mp4

# 降码率（适配移动端）
"$FFMPEG" -y -i input.mp4 -c:v h264_qsv -b:v 4M -c:a aac -b:a 128k output_mobile.mp4
```

---

## 5. 效率优化与自动化

### 操作说明
标注每个环节的自动化程度，提供批量脚本模板。

### 5.1 自动化程度矩阵

| 阶段 | 环节 | 自动化 | 工具/方法 |
|---|---|---|---|
| 搜集 | 关键词搜索 | ✅ 全自动 | `bilisearch:` |
| 搜集 | 格式筛选 | ✅ 全自动 | `yt-dlp -F` |
| 搜集 | 视频下载 | ✅ 全自动 | `yt-dlp -f` |
| 搜集 | 内容确认 | ⚠️ 半自动 | 截帧 + 人工查看 |
| 整理 | 目录创建 | ✅ 全自动 | `mkdir -p` |
| 整理 | 素材清单 | ⚠️ 半自动 | 模板 + 人工填 |
| 剪辑 | 分镜设计 | ❌ 人工 | 创意工作 |
| 剪辑 | 片段截取 | ✅ 全自动 | 批量 ffmpeg 脚本 |
| 剪辑 | 去水印 | ✅ 全自动 | delogo 滤镜 |
| 剪辑 | 拼接 | ✅ 全自动 | concat demuxer |
| 导出 | 编码合成 | ✅ 全自动 | ffmpeg 一条命令 |
| 导出 | 版本管理 | ✅ 全自动 | 文件命名规范 |

### 5.2 批量截取脚本模板

```bash
#!/bin/bash
FFMPEG="<ffmpeg路径>"
cd "<项目根>"

# 分镜表（序号|源|入点|时长|delogo参数）
CLIPS=(
  "01|BV1DHWcz8EQE.mp4|360|22|delogo=x=40:y=40:w=340:h=120"
  "02|BV11o4y1o7UK.mp4|120|20|delogo=x=1720:y=20:w=165:h=125"
  "03|BV11o4y1o7UK.mp4|270|20|delogo=x=1720:y=20:w=165:h=125"
  # ... 更多片段
)

mkdir -p clips
for clip in "${CLIPS[@]}"; do
  IFS='|' read -r NUM SRC SS T LOGO <<< "$clip"
  "$FFMPEG" -y -ss $SS -i "assets/raw/$SRC" -t $T \
    -vf "scale=1920:1080,fps=25,format=yuv420p,$LOGO" \
    -c:v h264_qsv -preset fast -global_quality 20 -an \
    "clips/${NUM}.mp4" 2>&1 | tail -1
done

# 拼接
printf "file 'clips/%02d.mp4'\n" $(seq -w 1 ${#CLIPS[@]}) > clips.txt
"$FFMPEG" -y -f concat -safe 0 -i clips.txt -c copy output/concat.mp4
```

### 5.3 后台运行（长任务）

```bash
# 下载/编码用后台运行，避免阻塞
# Bash 工具 run_in_background=true
# 完成后自动通知，用 TaskOutput 读取结果
```

### 5.4 性能对比（实测）

| 编码方案 | 1080P 4分钟耗时 | CPU占用 | 文件大小 | 质量 |
|---|---|---|---|---|
| libx264 -preset fast -crf 18 | 60s | ~90% | 244MB | 最好 |
| **h264_qsv -preset fast -gq 20** | **18s** | **~20%** | **166MB** | 良好 |
| h264_qsv -preset veryfast | 12s | ~15% | 150MB | 可接受 |
| **h264_nvenc -cq 20 -preset p5**（v1.1 实测）| 1440p60+字幕 134s 素材 49s（2.8x）| ~5-10% | 725MB(1440p60) | 高 |

---

## 6. 常见问题与陷阱

### 6.1 ffmpeg delogo 边界 bug
**问题**：`delogo=x=1300:w=620`（x+w=1920=画面宽度）报 `Failed to configure input pad`。
**解决**：让 `x+w < 画面宽度`，如 `x=1280:w=620`（x+w=1900）。

### 6.2 变帧率导致播放卡顿
**问题**：多源拼接后输出变帧率（如 18.64fps），播放器卡顿。
**解决**：截取时强制 `fps=25`（CFR恒定帧率），不要用 `-c copy` 流复制保留原帧率。

### 6.3 混合编码 concat 失败
**问题**：H.264 + AV1 混合源 concat 报错。
**解决**：所有片段先统一编码为 H.264/yuv420p 再拼接。

### 6.4 yt-dlp cookies 提取失败
**问题**：`--cookies-from-browser edge` 报 `Could not copy Chrome cookie database`。
**解决**：关闭 Edge 浏览器后重试；或手动导出 cookies.txt。

### 6.5 concat 相对路径找不到文件
**问题**：`clips.txt` 用相对路径 `clips/01.mp4`，ffmpeg 报 `Impossible to open`。
**解决**：用绝对路径，或确认 ffmpeg 工作目录与 clips.txt 一致。

### 6.6 QSV 编码与 delogo 兼容
**实测**：h264_qsv + delogo 滤镜正常工作，但 delogo 区域不能贴画面边界。

### 6.7 去水印残留
**问题**：delogo 对完全不透明水印（白底蓝字）效果有限。
**解决**：
- 扩大 delogo 区域（但避开边界）
- 或用 AI inpainting（ProPainter/E2FGVI，需 Python 推理环境）
- 或裁剪画面边缘

### 6.8 pip 清华源 403（v1.1 实测）
**问题**：`-i https://pypi.tuna.tsinghua.edu.cn/simple` 安装 imageio-ffmpeg 等小包时报 `HTTP 403 Forbidden`。
**解决**：去掉 `-i` 裸跑 `pip install <包名>`（官方源），或换阿里源 `-i https://mirrors.aliyun.com/pypi/simple`。

### 6.9 subtitles 滤镜 Windows 路径（v1.1 实测）
**问题**：`-vf "subtitles=C:/Users/xx/sub.srt"` 报错（冒号/反斜杠被滤镜解析器误读）。
**解决**：先 `cd` 到字幕所在目录，用相对文件名 `subtitles=sub.srt`；或对 `:` 转义 `C\\:/Users/...`。

### 6.10 concat 流复制时画面顺序错乱/无法打开
**问题**：`-c copy` 拼接后播放器快进错乱或末尾花屏。
**解决**：拼接前确认各段 SAR/分辨率/像素格式一致；输出务必加 `-movflags +faststart`；个别播放器兼容问题改用 `-c:v h264_nvenc` 重新编码一版。

---

## 附录 A：命令速查

### 一键完整流程（QSV版）
```bash
FFMPEG="<ffmpeg路径>"; cd "<项目根>"

# 1. 截取12个片段
for clip in "01|src1|300|20|logo1" "02|src2|120|20|logo2"; do
  IFS='|' read -r NUM SRC SS T LOGO <<< "$clip"
  "$FFMPEG" -y -ss $SS -i "assets/raw/$SRC.mp4" -t $T \
    -vf "scale=1920:1080,fps=25,format=yuv420p,$LOGO" \
    -c:v h264_qsv -preset fast -global_quality 20 -an "clips/$NUM.mp4"
done

# 2. 拼接
printf "file 'clips/%02d.mp4'\n" {1..12} > clips.txt
"$FFMPEG" -y -f concat -safe 0 -i clips.txt -c copy output/concat.mp4

# 3. 合成BGM+fade
"$FFMPEG" -y -i output/concat.mp4 -i assets/audio/bgm.mp3 \
  -t 243 -c:v h264_qsv -preset fast -global_quality 20 -pix_fmt yuv420p \
  -vf "fade=t=out:st=230:d=13" -c:a aac -b:a 192k \
  -af "afade=t=out:st=230:d=13" -movflags +faststart \
  output/final_v1.mp4
```

### 中文字幕生成与烧录（v1.1）
```bash
# 1. 生成 SRT（Python，时间轴按 durations 自动累计）
#    durations=[18.03,...]; lines=["文案1",...] → 累计 starts → 写 srt
# 2. 烧录
cd output && "$FFMPEG" -y -i concat.mp4 \
  -vf "subtitles=subs.srt:force_style='FontName=Microsoft YaHei,FontSize=22,PrimaryColour=&H00FFFFFF,OutlineColour=&H00000000,BorderStyle=1,Outline=2,Shadow=1,MarginV=80'" \
  -c:v h264_nvenc -rc vbr -cq 20 -b:v 0 -preset p5 -c:a copy -movflags +faststart final_v2_sub.mp4
```

### 截帧分析
```bash
for t in 00:00:15 00:02:00 00:04:30 00:07:00; do
  "$FFMPEG" -y -ss $t -i <源>.mp4 -frames:v 1 -q:v 3 -vf scale=480:-1 "frames/<源>_${t//:/}.jpg"
done
```

### 水印位置测量
```bash
# 右上角
"$FFMPEG" -y -ss 30 -i <源>.mp4 -frames:v 1 -vf "crop=300:200:1620:0,scale=1200:800" corner_right.jpg
# 左上角
"$FFMPEG" -y -ss 30 -i <源>.mp4 -frames:v 1 -vf "crop=300:200:0:0,scale=1200:800" corner_left.jpg
```

---

## 附录 B：本项目实例参数

| 项 | 值 |
|---|---|
| 项目 | 皇马欧冠三连冠怀念视频 |
| 配乐 | 《去年夏天》王大毛，4:03，128 BPM |
| 成片时长 | 4:03 |
| 分辨率 | 1920×1080 |
| 帧率 | 25fps CFR |
| 编码 | H.264 High，h264_qsv，global_quality 20 |
| 视频码率 | ~5300 kb/s |
| 音频 | AAC 192k，44.1kHz stereo |
| 文件大小 | 166MB |
| 编码耗时 | 18秒（QSV）vs 60秒（x264） |
| 素材源 | B站 3个视频（BV11o4y1o7UK + BV1DHWcz8EQE + BV1at4y1p7Xv）|
| 剪辑结构 | 两段式：进球集锦(0:00-2:00) + 怀念回忆(2:00-4:03) |
| 水印处理 | delogo 去除 3个源的水印 |

### 项目实例 2：Dota2 先知（Nature's Prophet）集锦（v1.1 新增）

| 项 | 值 |
|---|---|
| 项目 | Dota2 先知集锦（10 个已切片素材直拼）|
| 硬件环境 | 笔记本 i5-12500H + RTX 3050 Laptop 4GB（NVENC）|
| 素材 | C:/Users/Lenovo/Documents/dota_clips/clip1~10.mp4（10 段共 134.55s）|
| 素材规格 | 2560×1440 / 60fps / H.264 Main / yuv420p / AAC 160k（参数全一致）|
| 处理路径 | concat `-c copy` + faststart 流复制直拼（不重编码，秒级）|
| 成片 v1 | prophet_highlights_v1.mp4，489MB，2:14.55 |
| 字幕版 v2 | prophet_highlights_v2_sub.mp4，725MB（NVENC -cq 20，45Mbps）|
| 音频策略 | 保留原声（游戏音效），不加 BGM |
| 字幕 | 10 段分镜文案 SRT（外挂）+ 烧录版；微软雅黑 22 号白字黑边 |
| 编码耗时 | 流复制 ~3s；NVENC 烧录字幕 49s（2.8x）|

**分镜文案**（中二高燃风，可直接套用或改写）：
开局打野「大树也能 carry 全场」→ 支援「随叫随到」→ 斩杀「一刀带走」→「字典里没有撤退」→「树人军团，出击」→ 推塔「我只是在认真上班」→ 团战「五个人也拦不住我」→ 肉山「谁来管管这棵树」→ 大招「自然之怒，席卷全图」→ 收割「万物皆可斩」→ 收束「集锦 · 完」

---

*文档版本：v1.1 | 基于 WorkBuddy 2026-07-27 皇马欧冠项目 + 2026-08-12 Dota2 先知集锦项目 | 持续迭代*

> v1.1 新增：环境切换（QSV→NVENC）经验、已切片素材直拼（流复制）、中文字幕烧录（3.8）、Dota2 先知集锦实例参数（附录 B）
